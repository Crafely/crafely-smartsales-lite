<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="Crafely SmartSales" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/PHPCSStandards/PHP_CodeSniffer/master/phpcs.xsd">

    <description>Coding standards for Crafely SmartSales WordPress Plugin</description>

    <!-- Scan these files -->
    <file>.</file>

    <!-- Exclude Paths -->
    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>/node_modules/*</exclude-pattern>
    <exclude-pattern>/assets/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>
    <exclude-pattern>/tests/*</exclude-pattern>

    <!-- How to scan -->
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="colors"/>
    <arg name="basepath" value="."/>
    <arg name="extensions" value="php"/>
    
    <!-- Enable fixes for spacing, indentation, and other formatting -->
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="1"/>
            <property name="exact" value="true"/>
            <property name="tabIndent" value="true"/>
        </properties>
    </rule>

    <!-- Force tab-based indentation -->
    <arg name="tab-width" value="4"/>
    <rule ref="Generic.WhiteSpace.DisallowSpaceIndent"/>
    
    <!-- Configure spacing rules -->
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
        <properties>
            <property name="ignoreBlankLines" value="false"/>
        </properties>
    </rule>

    <!-- Set tab width -->
    <arg name="tab-width" value="4"/>

    <!-- Show progress of the run -->
    <arg value="p"/>

    <!-- Enable parallel processing -->
    <arg name="parallel" value="8"/>
    
    <!-- Configure brace and parenthesis rules -->
    <rule ref="Generic.Functions.OpeningFunctionBraceKernighanRitchie"/>
    <rule ref="Squiz.Functions.MultiLineFunctionDeclaration"/>

    <!-- Rules -->
    <rule ref="WordPress-Core">
        <!-- Allow modern PHP features -->
        <exclude name="WordPress.Files.FileName"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName"/>
        <!-- <exclude name="Generic.WhiteSpace.DisallowSpaceIndent"/> -->
        <exclude name="Universal.Arrays.DisallowShortArraySyntax"/>
        <exclude name="PEAR.Functions.FunctionCallSignature"/>
        <!-- <exclude name="Generic.Functions.OpeningFunctionBraceKernighanRitchie"/> -->
        <exclude name="WordPress.NamingConventions.PrefixAllGlobals"/>
    </rule>

    <!-- PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>

    <!-- WordPress Compatibility -->
    <config name="minimum_wp_version" value="6.0"/>
    
    <!-- Add memory limit for PHPCS -->
    <ini name="memory_limit" value="256M"/>

    <!-- Plugin specific rules -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="crafely-smartsales-lite"/>
            </property>
        </properties>
    </rule>

    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="csmsl"/>
            </property>
        </properties>
    </rule>

    <!-- PSR-4 Compatible Namespaces -->
    <rule ref="PSR1.Classes.ClassDeclaration"/>
</ruleset>